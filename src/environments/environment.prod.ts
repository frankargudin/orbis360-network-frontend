export const environment = {
  production: true,
  // WebSocket URL — set this to your Railway backend URL before deploying
  // Example: 'https://orbis360-api-production.up.railway.app'
  // Leave empty to use same-host (if using Vercel rewrites for HTTP)
  wsUrl: '',
};
